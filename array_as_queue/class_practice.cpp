// a bank has multiple ATMs serving a large number of custormers. The ATM system needs to manage the queue of customers waiting for service. Each customer waits in line to use the ATM, and they are served in the order they arrive (first-come, first-served).

// The system needs to ensure:

// -->Fairness: The customers who arrives first should be served first.
// -->Efficient Service: As soon as one customer finishes their transaction, the next customer in the queue.